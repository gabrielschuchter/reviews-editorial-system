# Contratos de provenance

`source-roles.json` mantém `source_id`, `source_role`, `authority_rank`, `location` e `rationale`. Papéis de exemplo nunca autorizam afirmação factual. Fontes externas são contexto ou escrutínio; não substituem fonte original. O claim ledger continua sendo a única autorização para afirmação pública factual.
